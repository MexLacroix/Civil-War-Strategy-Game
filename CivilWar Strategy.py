
class Piece:
    def __init__(self, name, offensive, deffensive, movement, sprite, people, )
        self.offensive = offensive
        self.deffensive = deffensive
        self.movement = movement
        self.sprite = sprite
        self.people = people


def Find_list(piece_list):
    militia_num = str.piece_list.count(militia)
    infantry_num = str.piece_list.count(infantry)
    artillery_num = str.piece_list.count(artillery)
    cavalry_num = str.piece_list.count(cavalry)
    sharpshooter_num = str.piece_list.count(sharpshooter)
    gunboat_num = str.piece_list.count(gunboat)


    return militia_num, infantry_num, artillery_num, cavalry_num, sharpshooter_num, gunboat_num   
    
    

def fight(attacking_peices, defending_peices):
    print('Battle start!')
    print(
    



    
    
